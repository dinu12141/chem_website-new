version: '3.8'

services:
  mongodb:
    image: mongo:7.0
    container_name: nadeeka-warnakula-mongodb
    restart: unless-stopped
    environment:
      MONGO_INITDB_DATABASE: nadeeka_warnakula
    ports:
      - "27017:27017"
    volumes:
      - mongodb_data:/data/db
      - ./backend/init-mongo.js:/docker-entrypoint-initdb.d/init-mongo.js:ro
    networks:
      - nadeeka-warnakula-network

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: nadeeka-warnakula-backend
    restart: unless-stopped
    environment:
      - MONGO_URL=mongodb://mongodb:27017
      - DB_NAME=nadeeka_warnakula
      - CORS_ORIGINS=http://localhost:3000,http://frontend:3000
      - SECRET_KEY=nadeeka_warnakula_secret_key_2024_nadeeka_warnakula
      - ACCESS_TOKEN_EXPIRE_MINUTES=43200
      - HOST=0.0.0.0
      - PORT=8000
      - ENVIRONMENT=production
      - LOG_LEVEL=INFO
    ports:
      - "8000:8000"
    depends_on:
      - mongodb
    networks:
      - nadeeka-warnakula-network
    volumes:
      - ./backend/.env:/app/.env:ro

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: nadeeka-warnakula-frontend
    restart: unless-stopped
    environment:
      - REACT_APP_BACKEND_URL=http://localhost:8000
      - REACT_APP_API_URL=http://localhost:8000/api
      - GENERATE_SOURCEMAP=false
      - REACT_APP_ENVIRONMENT=production
    ports:
      - "3000:3000"
    depends_on:
      - backend
    networks:
      - nadeeka-warnakula-network

volumes:
  mongodb_data:
    driver: local

networks:
  nadeeka-warnakula-network:
    driver: bridge